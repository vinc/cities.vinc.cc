<div class="row map-wrapper">
  <div class="col-sm-4" id="legend">
    <h1>Cities</h1>
    <ul>
      <% cities.each do |city| %>
        <li><%= link_to(city.title, city) %></li>
      <% end%>
    </ul>
  </div>
  <div class="col-sm-8" id="map">
    <script>
      genMap(<%= cities.map { |city| { city: city } }.to_json.html_safe %>);
    </script>
  </div>
</div>
