<% content_for(:title, 'Search best cities') %>

<h1>Results</h1>
<p><%= pluralize(cities.count, 'city') %></p>
<ul>
  <% cities.each do |city| %>
    <li><%= link_to(city.title, city) %></li>
  <% end%>
</ul>

<hr>

<h2>Search</h2>

<%= render 'form_search' %>

<hr>
